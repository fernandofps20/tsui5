specVersion: "2.0"
metadata:
  name: <%= props.appId %>
type: application
resources:
  configuration:
    paths:
      webapp: dist
framework:
  name: <%= props.framework %>
  version: <%= props.frameworkVersion %>
  libraries:
    - name: sap.m
    - name: sap.ui.core
    - name: sap.ui.unified
    - name: themelib_sap_fiori_3